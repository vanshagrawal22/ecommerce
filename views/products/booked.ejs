<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Processing</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #121212;
            color: #FFFFFF;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: 'Arial', sans-serif;
        }
        .success-container {
            text-align: center;
            padding: 2rem;
            border-radius: 15px;
            background-color: #1e1e1e;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.7);
            width: 80%;
            max-width: 500px;
            animation: fadeIn 1s ease-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(50px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .success-container h1 {
            color: #ffb347;
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
        }
        .success-container p {
            font-size: 1.2rem;
            margin-bottom: 1rem;
        }
        .price-info {
            font-size: 1.5rem;
            font-weight: bold;
            color: #ff6347;
            margin-bottom: 1.5rem;
        }
        .btn-home {
            padding: 0.75rem 1.5rem;
            font-size: 1.2rem;
            background-color: #FF4C4C;
            color: white;
            text-decoration: none;
            border-radius: 10px;
            transition: background-color 0.3s;
        }
        .btn-home:hover {
            background-color: #e63946;
        }
        .processing-indicator {
            font-size: 1.5rem;
            font-weight: bold;
            color: #ffd700;
            margin-bottom: 2rem;
            animation: blink 1.5s infinite;
        }
        @keyframes blink {
            0% { opacity: 0.5; }
            50% { opacity: 1; }
            100% { opacity: 0.5; }
        }
    </style>
</head>
<body>
    <div class="success-container">
        <h1>🚀 Processing Your Booking... 🚀</h1>
        <p class="processing-indicator">Please wait, your payment is being processed!</p>
        <p>Thank you, <%= name %>. Your ticket is being processed!</p>
        <p class="price-info">Price: ₹<%= price %></p>
        <form id="stripe-form" action="/checkout" method="POST">
            <input type="hidden" name="productId" value="<%= productId %>">
            <input type="hidden" name="name" value="<%= name %>">
            <input type="hidden" name="price" value="<%= price %>">
            <button type="submit" class="btn btn-primary btn-home">Proceed to Payment</button>
        </form>
    </div>
</body>
</html>